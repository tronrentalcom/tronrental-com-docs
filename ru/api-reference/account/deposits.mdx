---
title: Депозиты
description: "Пополнение аккаунта в TRX или USDT"
---

## Получить адрес для депозита

<div>
  `GET /api/v1/account/deposit`
</div>

Возвращает ваш персональный адрес для пополнения и принимаемые валюты.

```bash
curl https://api.tronrental.com/v1/account/deposit \
  -H "X-API-Key: your_api_key"
```

```json
{
  "deposit_address": "TYourDepositAddress...",
  "accepted_currencies": ["TRX", "USDT"],
  "usdt_to_trx_rate": "26.5",
  "trx_usd_rate": "0.234"
}
```

## Список депозитов

<div>
  `GET /api/v1/account/deposits`
</div>

Возвращает историю ваших депозитов.

```bash
curl https://api.tronrental.com/v1/account/deposits \
  -H "X-API-Key: your_api_key"
```

### Параметры запроса

<ParamField query="from" type="string">
  Первый день периода, `YYYY-MM-DD`, включительно. Без обеих дат — весь список.
</ParamField>

<ParamField query="to" type="string">
  Последний день периода, `YYYY-MM-DD`, **включительно** — учитываются все сутки, до 23:59:59.999 в вашей таймзоне.
</ParamField>

<ParamField query="tz" type="string">
  Таймзона IANA, в которой читаются обе даты, например `Europe/Berlin`. Без неё — UTC.
</ParamField>

<Info>
  В отличие от отчёта, у списка нет ограничения в один год — он постраничный. Обе даты необязательны: только `from` означает «с этого дня и дальше», только `to` — «по этот день включительно».
</Info>

### Статусы депозитов

| Статус | Описание |
|--------|----------|
| `pending` | Транзакция обнаружена, ожидание подтверждения |
| `confirmed` | Зачислено на ваш баланс |
| `expired` | Адрес для депозита истёк |

<Info>
  Депозиты зачисляются автоматически. Депозиты в TRX зачисляются 1:1. Депозиты в USDT конвертируются в TRX по текущему рыночному курсу.
</Info>
