---
title: Listar Faturas
api: GET /api/v1/invoices/my
description: "Listar suas faturas com filtragem opcional"
---

## Listar Faturas

Retorna uma lista paginada das suas faturas.

### Parâmetros de consulta

<ParamField query="page" type="number" default="1">
  Número da página
</ParamField>

<ParamField query="status" type="string">
  Filtrar por status: `pending`, `paid`, `delegated`, `expired`, `failed`
</ParamField>

<ParamField query="from" type="string">
  Primeiro dia do período, `YYYY-MM-DD`, inclusive. Sem nenhuma das duas datas, a lista completa.
</ParamField>

<ParamField query="to" type="string">
  Último dia do período, `YYYY-MM-DD`, **inclusive** — o dia inteiro conta, até 23:59:59.999 no seu fuso horário.
</ParamField>

<ParamField query="tz" type="string">
  Fuso horário IANA em que as duas datas são lidas, por exemplo `Europe/Berlin`. Omitido significa UTC.
</ParamField>

<Info>
  Ao contrário de um relatório, a lista não tem limite de um ano — ela é paginada. Ambas as datas são opcionais: apenas `from` significa «desse dia em diante»; apenas `to`, «até esse dia inclusive».
</Info>

### Resposta

Retorna um objeto paginado com array `invoices` e contagem `total`.

### Exemplo

```bash
curl "https://api.tronrental.com/v1/invoices/my?page=1&status=delegated" \
  -H "X-API-Key: your_api_key"
```

```json Response
{
  "invoices": [
    {
      "invoice_id": "265ba22d-6c76-4ec5-820c-d2ad153be42f",
      "payment_address": "TPaymentAddress...",
      "target_address": "TRecipientAddress...",
      "resource": "energy",
      "energy_amount": 65000,
      "transfer_count": 1,
      "duration": null,
      "price_trx": "2.150000",
      "price_usdt": "0.740000",
      "status": "delegated",
      "expires_at": "2026-03-05T12:30:00Z",
      "created_at": "2026-03-05T12:00:00Z",
      "paid_at": null,
      "completed_at": null
    }
  ],
  "total": 1,
  "page": 1,
  "page_size": 20
}
```
