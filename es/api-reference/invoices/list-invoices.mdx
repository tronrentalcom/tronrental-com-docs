---
title: Listar facturas
api: GET /api/v1/invoices/my
description: "Listar tus facturas con filtrado opcional"
---

## Listar facturas

Devuelve una lista paginada de tus facturas.

### Parametros de consulta

<ParamField query="page" type="number" default="1">
  Numero de pagina
</ParamField>

<ParamField query="status" type="string">
  Filtrar por estado: `pending`, `paid`, `delegated`, `expired`, `failed`
</ParamField>

<ParamField query="from" type="string">
  Primer día del período, `YYYY-MM-DD`, inclusive. Sin ninguna de las dos fechas, la lista completa.
</ParamField>

<ParamField query="to" type="string">
  Último día del período, `YYYY-MM-DD`, **inclusive**: cuenta el día entero, hasta las 23:59:59.999 en tu zona horaria.
</ParamField>

<ParamField query="tz" type="string">
  Zona horaria IANA en la que se leen ambas fechas, por ejemplo `Europe/Berlin`. Si se omite, UTC.
</ParamField>

<Info>
  A diferencia de un informe, la lista no tiene un límite de un año: está paginada. Ambas fechas son opcionales: solo `from` significa «desde ese día en adelante»; solo `to`, «hasta ese día incluido».
</Info>

### Respuesta

Devuelve un objeto paginado con un array `invoices` y el conteo `total`.

### Ejemplo

```bash
curl "https://api.tronrental.com/v1/invoices/my?page=1&status=delegated" \
  -H "X-API-Key: your_api_key"
```

```json Response
{
  "invoices": [
    {
      "invoice_id": "265ba22d-6c76-4ec5-820c-d2ad153be42f",
      "payment_address": "TPaymentAddress...",
      "target_address": "TRecipientAddress...",
      "resource": "energy",
      "energy_amount": 65000,
      "transfer_count": 1,
      "duration": null,
      "price_trx": "2.150000",
      "price_usdt": "0.740000",
      "status": "delegated",
      "expires_at": "2026-03-05T12:30:00Z",
      "created_at": "2026-03-05T12:00:00Z",
      "paid_at": null,
      "completed_at": null
    }
  ],
  "total": 1,
  "page": 1,
  "page_size": 20
}
```
